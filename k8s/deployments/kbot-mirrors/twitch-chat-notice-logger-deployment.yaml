apiVersion: apps/v1
kind: Deployment
metadata:
  name: kbot-mirrors-twitch-chat-notice-logger
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kbot-mirrors-twitch-chat-notice-logger
  template:
    metadata:
      labels:
        app: kbot-mirrors-twitch-chat-notice-logger
    spec:
      containers:
        - name: kbot-mirrors-twitch-chat-notice-logger
          image: docker-registry.app.kunszg.com/kbot-mirrors:latest
          imagePullPolicy: Always
          volumeMounts:
            - name: node-modules-volume
              mountPath: /usr/src/app/node_modules
          envFrom:
            - configMapRef:
                name: kbot-config
          env:
            - name: APP_NAME
              value: "TWITCH_CHAT_NOTICE_LOGGER"
      imagePullSecrets:
        - name: regcred-v1
      volumes:
        - name: node-modules-volume
          hostPath:
            path: /home/docker/kbot/node_modules
            type: Directory
