apiVersion: apps/v1
kind: Deployment
metadata:
  name: kbot-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kbot-backend
  template:
    metadata:
      labels:
        app: kbot-backend
    spec:
      containers:
        - name: kbot-backend
          image: docker-registry.kunszg.com/kbot-backend:latest
          imagePullPolicy: Always
          volumeMounts:
            - name: node-modules-volume
              mountPath: /usr/src/app/node_modules
          envFrom:
            - configMapRef:
                name: kbot-config
      imagePullSecrets:
        - name: regcred-v1
      volumes:
        - name: node-modules-volume
          hostPath:
            path: /home/docker/kbot/node_modules
            type: Directory
