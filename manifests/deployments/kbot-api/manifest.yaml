apiVersion: apps/v1
kind: Deployment
metadata:
  name: kbot-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kbot-api
  template:
    metadata:
      labels:
        app: kbot-api
    spec:
      containers:
        - name: kbot-api
          image: docker-registry.kunszg.com/kbot-api:latest
          imagePullPolicy: Always
          volumeMounts:
            - name: node-modules-volume
              mountPath: /usr/src/app/node_modules
          envFrom:
            - configMapRef:
                name: kbot-config
      imagePullSecrets:
        - name: regcred-v1
      volumes:
        - name: node-modules-volume
          hostPath:
            path: /home/docker/node_modules
            type: Directory
